[phases.build]
cmds = [
  "npm install",
  "GENERATE_SOURCEMAP=false npm run build"
]

[phases.start]
cmd = "cd src/backend && npm start"

[variables]
NODE_ENV = "production"
GENERATE_SOURCEMAP = "false" 