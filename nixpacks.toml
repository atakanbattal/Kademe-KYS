[phases.build]
cmds = [
  "cd src/backend && npm install",
  "cd src/backend && npm run build"
]

[phases.start]
cmd = "cd src/backend && npm start"

[variables]
NODE_ENV = "production"

# DEPLOYMENT TRIGGER - 2024-12-23 15:30:00 UTC
# Critical data loss fix applied - backend updated 