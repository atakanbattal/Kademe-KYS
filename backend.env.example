# MongoDB Atlas Connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/kademe-kys?retryWrites=true&w=majority

# JWT Secret (Production'da güvenli bir key kullanın)
JWT_SECRET=your_super_secure_jwt_secret_key_here

# Node Environment
NODE_ENV=production

# Port (Railway otomatik ayarlayacak)
PORT=5000

# CORS Origins (Frontend URL'leri)
CORS_ORIGINS=https://your-netlify-app.netlify.app,http://localhost:3000 